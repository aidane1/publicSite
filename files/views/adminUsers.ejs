<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <meta name = "viewport" content = "width=device-width, initial-scale=1.0" />
    <title></title>
    <style>

      * {
        font-family: sans-serif;
        box-sizing: border-box;
      }
      body, html {
        width: 100%;
        height: 100%;
        margin: 0;
      }
      .admin {
        padding: 20px;
        width: 200px;
        text-align: center;
        border: 1px solid black;
        position: relative;
      }

      .container {
        width: 100%;
        height: 100%;
        padding: 50px;
        overflow: auto;
      }
      .remove {
        position: absolute;
        top: -10px;
        right: -30px;
        color: red;
      }
    </style>
  </head>
  <body>
    <div class = "container">
      <% for (var i = 0; i < admins.length; i++) { %>
        <div class = "admin">
          <%=admins[i].username%>
          <div class = "remove" onclick = "removeAdmin('<%=admins[i]._id')"><h1>X</h1></div>
        </div>
      <% } %>
    </div>

  </body>
  <script>
    function removeAdmin(id) {
      let form = document.createElement("form");
      form.action = "/admin/user-permissions";
      form.method = "POST";
      let input = document.createElement("input");
      input.type = "hidden";
      input.value = id;
      input.name = "removedAdmin";
      form.appendChild(input);
      document.body.appendChild(form);
      form.submit();
    }
  </script>
</html>
